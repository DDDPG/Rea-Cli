# Source archive index

Full source snapshots, including ReaperDoc ignored supplements, are preserved locally under
`.state/ecosystem-baseline-20260913/`. They are deliberately not publication payload.
The imported ReaperDoc Git history is a parent of the monorepo import commit.
Adopted synthetic API/save evidence is in `schema/rpp/evidence`.
`docs/ecosystem/baseline.json` and `corpus.json` identify the baseline and related local corpus.
Original sibling source directories are retained; no user media was moved or deleted.
Historical TS parameter data in apps/reaperdoc is frozen provenance, not a second editable specification.
