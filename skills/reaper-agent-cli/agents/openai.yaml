interface:
  display_name: "REAPER Agent CLI"
  short_description: "Build and verify REAPER projects with rac, Lua and RPP"
  default_prompt: "Use $reaper-agent-cli to build or edit this REAPER project and verify the saved result."
policy:
  allow_implicit_invocation: true
